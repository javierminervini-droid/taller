# Copy to .env and adjust as needed:
#   cp .env.example .env

POSTGRES_USER=taller
POSTGRES_PASSWORD=taller
POSTGRES_DB=taller_gestion
POSTGRES_PORT=5432

# Used by the Go API when running on the host (make api)
DATABASE_URL=postgres://taller:taller@127.0.0.1:5432/taller_gestion?sslmode=disable

JWT_SECRET=taller-gestion-local-dev
PORT=3847
API_PORT=3847
CORS_ORIGIN=*
RUN_SEED=true
